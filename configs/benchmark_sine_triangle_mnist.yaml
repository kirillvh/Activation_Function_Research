benchmark:
  name: sine_triangle_mnist
  base_config: configs/research_mnist.yaml
  output_dir: runs/benchmarks/research
  repeats: 3
  matrix:
    model.activation:
      - gelu
      - gelu_sine_triangle
    training.activation_learning_rate_multiplier: [1.0]
