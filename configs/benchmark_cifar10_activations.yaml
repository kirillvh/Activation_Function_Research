benchmark:
  name: cifar10_activations
  base_config: configs/cifar10.yaml
  output_dir: runs/benchmarks/cifar10
  repeats: 1
  matrix:
    model.activation: [peuaf, relu, gelu, silu, tanh]
    training.epochs: [256]
