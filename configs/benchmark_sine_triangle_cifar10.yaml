benchmark:
  name: sine_triangle_cifar10
  base_config: configs/research_cifar10.yaml
  output_dir: runs/benchmarks/research
  repeats: 3
  matrix:
    model.activation:
      - gelu
      - gelu_sine_triangle
    training.activation_learning_rate_multiplier: [1.0]
